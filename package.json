{
  "name": "react-walking-data",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && mkdir build && browserify src/main.js -o build/bundle.js -d -t reactify && cp -r node_modules/bootstrap build && cp -r node_modules/react-bootstrap-daterangepicker build"
  },
  "author": "me@lfac.me",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^3.3.4",
    "browserify": "^9.0.7",
    "d3": "^3.5.5",
    "moment": "^2.9.0",
    "react": "^0.13.2",
    "react-bootstrap-daterangepicker": "^0.2.5",
    "reactify": "^1.1.0",
    "underscore": "^1.8.3"
  }
}
